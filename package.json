{
  "name": "imago-core",
  "private": true,
  "version": "0.1.0",
  "description": "Premium character-collection digital CCG where human identity is forged into collectible artifacts.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "battle:simulate": "node scripts/battle-simulate.js",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "latest",
    "wrangler": "latest"
  }
}
