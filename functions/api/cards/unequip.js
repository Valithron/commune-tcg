import{ensureGameSchema,getSessionUser,json}from'../../_shared/game.js';
export async function onRequestPost({request,env}){try{await ensureGameSchema(env);let user=await getSessionUser(request,env);if(!user)return json({error:'Not logged in'},401);let{id}=await request.json();let row=await env.DB.prepare('SELECT card_json FROM cards WHERE id=? AND owner_user_id=?').bind(String(id||''),user.id).first();if(!row)return json({error:'Card not found'},404);let card=JSON.parse(row.card_json);card.equipped=false;await env.DB.prepare("UPDATE cards SET card_json=?,updated_at=datetime('now') WHERE id=? AND owner_user_id=?").bind(JSON.stringify(card),card.id,user.id).run();return json({ok:true,card})}catch(e){return json({error:e.message||'Unequip failed'},500)}}
