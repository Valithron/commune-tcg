/* ============================================================================
   Mock User Data
   Phase 1 responsibility: provide stable local values for visual composition.
   Replace with account/session API data in a later backend phase.
   ============================================================================ */

export const mockUser = {
  displayName: 'Sterling',
  level: 7,
  pullTickets: 12,
  gold: 2840,
  vaultCount: 18,
  librarySeen: 64,
  dailyPullReady: true,
  streakDays: 5,
};
